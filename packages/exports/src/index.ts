// TODO (deferred — PLAN §2 "Deferred"): real export contracts (JSON / markdown / text / SVG)
// will be ported here. The placeholder exporter was removed in the cleanup pass so nothing
// fake is exposed from this package's build surface.
export {};
