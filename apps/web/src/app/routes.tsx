import { App } from "../App";

// TODO: Replace this simple route config with a real router when the migrated
// frontend gains deep links for graph, reports, settings, and file detail views.
export const routes = [
  {
    path: "/",
    element: <App />,
  },
];
