#!/usr/bin/env node

// TODO: Connect this command to the local/private analyzer pipeline in a later phase.
console.log("CodeFlow local/private mode CLI placeholder");
console.log("Private repo analysis will run locally in a later phase.");
