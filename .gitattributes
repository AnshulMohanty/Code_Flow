# Files that run inside Linux containers MUST keep LF endings regardless of host OS, or the
# shell chokes on \r. (Authored on Windows; built/run on Linux.)
*.sh text eol=lf
Dockerfile text eol=lf
*.Dockerfile text eol=lf
nginx.conf text eol=lf
